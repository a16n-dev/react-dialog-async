---
sidebar_position: 2
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';


# Installation

Install the package using your package manager of choice.

<Tabs>
  <TabItem value="yarn" label="yarn">

    ```powershell
    yarn add react-dialog-async
    ```

  </TabItem>
  <TabItem value="npm" label="npm">

    ```powershell
    npm install react-dialog-async
    ```

  </TabItem>
  <TabItem value="pnpm" label="pnpm">

    ```powershell
    pnpm install react-dialog-async
    ```

  </TabItem>
  <TabItem value="bun" label="bun">

    ```powershell
    bun add react-dialog-async
    ```

  </TabItem>
</Tabs>


---

### Wrap your app with `DialogProvider`

Add the provider `DialogProvider` to the root of your app

``` tsx
import { DialogProvider } from 'react-dialog-async';

export default function Layout() {
  return (
    <DialogProvider>
      <App />
    </DialogProvider>
  );
}
```

:::tip
Not sure where to put the provider? Check out some of the [examples](../category/examples)
:::
